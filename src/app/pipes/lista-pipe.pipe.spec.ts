import { ListaPipePipe } from './lista-pipe.pipe';

describe('ListaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ListaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
